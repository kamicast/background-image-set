{
  "name": "@iftt/background-image-set",
  "version": "1.0.0",
  "description": "A bunch of background 1080p images to take advantage of",
  "main": "lib/index.js",
  "scripts": {
    "lint+build+test": "yarn run lint && yarn run build && yarn run test",
    "lint": "echo linting... && eslint ./src/**/*.js && echo 'successfully linted'",
    "build": "echo building... && babel src/ -d lib/ && echo 'successfully built'",
    "prepublish": "yarn run build",
    "test": "echo running server tests... && tape test/**/*.js | tap-summary && echo 'successfully completed tests'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/iftt/background-image-set.git"
  },
  "author": "Craig OConnor",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/IFTT/background-image-set/issues"
  },
  "homepage": "https://github.com/IFTT/background-image-set#readme",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/plugin-syntax-flow": "^7.2.0",
    "@babel/preset-env": "^7.2.3",
    "@babel/preset-flow": "^7.0.0",
    "babel-eslint": "^10.0.1",
    "eslint": "^4.0.0",
    "flow-bin": "^0.48.0",
    "tape": "^4.6.3"
  }
}
